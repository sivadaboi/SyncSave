{
  "name": "savesync-relay",
  "version": "1.1.1",
  "description": "SaveSync WAN WebSocket relay server — standalone cloud deploy",
  "type": "module",
  "main": "relay-server.js",
  "scripts": {
    "start": "node relay-server.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
